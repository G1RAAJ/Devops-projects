pipeline {
    agent any

    environment {
        AWS_REGION = "us-east-2"
        ACCOUNT_ID = "652942059461"
        ECR_REPO_NAME = "webapp"
        CLUSTER_NAME = "webapp-cluster"
    }

    stages {

        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/G1RAAJ/Devops-projects.git'
            }
        }

        stage('Delete Helm Release') {
            steps {
                sh '''
                helm uninstall webapp || true
                '''
            }
        }

        stage('Delete Kubernetes Resources') {
            steps {
                sh '''
                kubectl delete svc webapp || true

                kubectl delete deployment webapp || true
                '''
            }
        }

        stage('Delete EKS Access Entry') {
            steps {
                sh '''
                aws eks delete-access-entry \
                --cluster-name $CLUSTER_NAME \
                --principal-arn arn:aws:iam::$ACCOUNT_ID:role/webapp-role \
                --region $AWS_REGION || true
                '''
            }
        }

        stage('Terraform Destroy') {
            steps {
                dir('webapp-project/terraform') {
                    sh '''
                    terraform init -upgrade

                    terraform destroy -auto-approve -var-file=terraform.tfvars
                    '''
                }
            }
        }

        stage('Delete ECR Repository') {
            steps {
                sh '''
                aws ecr delete-repository \
                --repository-name $ECR_REPO_NAME \
                --region $AWS_REGION \
                --force || true
                '''
            }
        }

        stage('Delete CloudWatch Log Group') {
            steps {
                sh '''
                aws logs delete-log-group \
                --log-group-name /aws/eks/$CLUSTER_NAME/cluster \
                --region $AWS_REGION || true
                '''
            }
        }

        stage('Delete KMS Alias') {
            steps {
                sh '''
                aws kms delete-alias \
                --alias-name alias/eks/$CLUSTER_NAME \
                --region $AWS_REGION || true
                '''
            }
        }

        stage('Verify Cleanup') {
            steps {
                sh '''
                echo "===== EKS Clusters ====="
                aws eks list-clusters --region $AWS_REGION

                echo "===== ECR Repositories ====="
                aws ecr describe-repositories --region $AWS_REGION || true
                '''
            }
        }
    }

    post {

        success {
            echo 'All Resources Destroyed Successfully'
        }

        failure {
            echo 'Destroy Pipeline Failed'
        }
    }
}
--------------------------------------------------------
Manually delete all resources 
------------------------------
✅ 1. Delete Helm Release
helm uninstall webapp

✅ 2. Delete Kubernetes Resources
kubectl delete all --all

✅ 3. Delete EKS Node Groups

aws eks list-nodegroups \
--cluster-name webapp-cluster \
--region us-east-2

Delete each nodegroup returned:

aws eks delete-nodegroup \
--cluster-name webapp-cluster \
--nodegroup-name default \
--region us-east-2

If actual name differs, use that exact name.

✅ 4. Verify Nodegroups Deleted
aws eks list-nodegroups \
--cluster-name webapp-cluster \
--region us-east-2

Wait until:

{
  "nodegroups": []
}

✅ 5. Delete EKS Cluster
aws eks delete-cluster \
--name webapp-cluster \
--region us-east-2

✅ 6. Delete ECR Repository
aws ecr delete-repository \
--repository-name webapp \
--region us-east-2 \
--force

✅ 7. Delete CloudWatch Logs
aws logs delete-log-group \
--log-group-name /aws/eks/webapp-cluster/cluster \
--region us-east-2

✅ 8. Delete KMS Alias
aws kms delete-alias \
--alias-name alias/eks/webapp-cluster \
--region us-east-2

✅ 9. Delete Load Balancers

aws elbv2 describe-load-balancers \
--region us-east-2

aws elbv2 delete-load-balancer \
--load-balancer-arn <LOAD_BALANCER_ARN>

✅ 10. Delete Security Groups (if remaining)

aws ec2 describe-security-groups \
--region us-east-2

aws ec2 delete-security-group \
--group-id <sg-id>

✅ 11. Delete VPC (Optional)

aws ec2 describe-vpcs \
--region us-east-2

aws ec2 delete-vpc \
--vpc-id <vpc-id>

🚀 FASTEST CLEAN CHECK

After cleanup verify:

EKS
aws eks list-clusters --region us-east-2

Expected:

{
  "clusters": []
}

ECR
aws ecr describe-repositories --region us-east-2
-----------------------
Delete Created Roles
-------------------
✅ Step 4 — Detach Policies
aws iam detach-role-policy \
--role-name webapp-role \
--policy-arn arn:aws:iam::aws:policy/AdministratorAccess

✅ Step 5 — Remove Instance Profile
aws iam list-instance-profiles-for-role \
--role-name webapp-role

aws iam remove-role-from-instance-profile \
--instance-profile-name webapp-role \
--role-name webapp-role

aws iam delete-instance-profile \
--instance-profile-name webapp-role

✅ Step 6 — Delete Role
aws iam delete-role \
--role-name webapp-role


🚀 Step 7 — Fastest Delete Role

aws iam detach-role-policy \
--role-name webapp-role \
--policy-arn arn:aws:iam::aws:policy/AdministratorAccess

aws iam delete-role \
--role-name webapp-role


---------------------------------------------------
To clear Jenkins Workspace
------------------------------

✅ Method 2 — Clean Using Shell Command

On Jenkins EC2:

cd /var/lib/jenkins/workspace
ls

Delete specific project workspace:

rm -rf webapp-project

OR delete all workspaces:

rm -rf /var/lib/jenkins/workspace/*

✅ Method 3 — Auto Clean Inside Jenkinsfile (Recommended)

Add this stage at beginning:

stage('Clean Workspace') {
    steps {
        cleanWs()
    }
}

⚠️ Plugin Required

Install plugin:
Workspace Cleanup Plugin
Inside Jenkins:

Manage Jenkins
→ Plugins
→ Available Plugins
→ Workspace Cleanup

✅ Alternative Without Plugin

Use shell command in Jenkinsfile:

stage('Clean Workspace') {
    steps {
        sh '''
        rm -rf *
        '''
    }
}

⚠️ CAREFUL

Only use:
rm -rf *

inside Jenkins workspace directory.

✅ Verify Workspace Cleaned

ls /var/lib/jenkins/workspace

🎯 Most Common Real Command

rm -rf /var/lib/jenkins/workspace/webapp-project/
------------------------------------------------------------
Problem solving commands
---------------------------------
🛠️Step 1: Delete Old EKS Cluster

Run:
aws eks delete-cluster \
--name webapp-cluster \
--region us-east-2

🛠️Step 2: Delete KMS Alias

Run:
aws kms delete-alias \
--alias-name alias/eks/webapp-cluster

🛠️Step 3: Delete CloudWatch Log Group

Run:
aws logs delete-log-group \
--log-group-name /aws/eks/webapp-cluster/cluster

🛠️Step 4: Clean Terraform Cache
Inside Jenkins EC2:

cd webapp-project/terraform

rm -rf .terraform
rm -f .terraform.lock.hcl

🛠️ Delete ASG
aws autoscaling delete-auto-scaling-group \
--auto-scaling-group-name <ASG_NAME> \
--force-delete \
--region us-east-2

🛠️ Then Terminate EC2 Instances

Get instance IDs:

aws ec2 describe-instances \
--region us-east-2 \
--query "Reservations[*].Instances[*].InstanceId"

Terminate:
aws ec2 terminate-instances \
--instance-ids i-xxxxxxxx \
--region us-east-2
[------------------------------------------------
if you get error like below is..

E0507 09:56:43.580768 9741 memcache.go:265] "Unhandled Error" err="couldn't get current server API group list: the server has asked for the client to provide credentials"

E0507 10:03:17.337224   10057 memcache.go:265] "Unhandled Error" err="couldn't get current server API group list: Get \"https://80BB074DBE72089C42A2DAE356A73F9C.gr7.us-east-2.eks.amazonaws.com/api?timeout=32s\": dial tcp: lookup 80BB074DBE72089C42A2DAE356A73F9C.gr7.us-east-2.eks.amazonaws.com on 172.31.0.2:53: no such host"

: dial tcp: lookup 80BB074DBE72089C42A2DAE356A73F9C.gr7.us-east-2.eks.amazonaws.com on 172.31.0.2:53: no such host"
Unable to connect to the server: dial tcp: lookup 80BB074DBE72089C42A2DAE356A73F9C.gr7.us-east-2.eks.amazonaws.com on 172.31.0.2:53: no such host
---------------------------]
install tools on sudo -i and perform task on sudo su - jenkins
Do step by step

aws sts get-caller-identity

🔹 Step 1 — Create EKS Access Entry
aws eks create-access-entry \
--cluster-name webapp-cluster \
--principal-arn arn:aws:iam::652942059461:role/webapp-role \
--region us-east-2

🔹 Step 2 — Attach EKS Admin Policy
aws eks associate-access-policy \
--cluster-name webapp-cluster \
--principal-arn arn:aws:iam::652942059461:role/webapp-role \
--policy-arn arn:aws:eks::aws:cluster-access-policy/AmazonEKSClusterAdminPolicy \
--access-scope type=cluster \
--region us-east-2

rm -rf ~/.kube/config

aws eks update-kubeconfig \
--region us-east-2 \
--name webapp-cluster

kubectl get nodes
------------------------------------
Error: [0m[0m[1mcreating KMS Alias (alias/eks/webapp-cluster): operation error KMS: CreateAlias, https response error StatusCode: 400, RequestID: 6672c084-99dd-4686-b8e0-e7761abc82a6, AlreadyExistsException: An alias with the name arn:aws:kms:us-east-2:652942059461:alias/eks/webapp-cluster already exists

Error: [0m[0m[1mcreating CloudWatch Logs Log Group (/aws/eks/webapp-cluster/cluster): operation error CloudWatch Logs: CreateLogGroup, https response error StatusCode: 400, RequestID: c6ca1e9e-e0a6-4ee3-af57-aa0fdc9fb997, ResourceAlreadyExistsException: The specified log group already exists

🔹 Delete KMS Alias
aws kms delete-alias \
--alias-name alias/eks/webapp-cluster \
--region us-east-2

🔹 Delete CloudWatch Log Group
aws logs delete-log-group \
--log-group-name /aws/eks/webapp-cluster/cluster \
--region us-east-2

🔹 Verify KMS Alias Deleted
aws kms list-aliases \
--region us-east-2

🔹 Verify Log Group Deleted
aws logs describe-log-groups \
--region us-east-2

Go to terraform directory:

cd webapp-project/terraform

rm -rf .terraform
rm -f .terraform.lock.hcl

terraform init -upgrade
terraform apply -auto-approve -var-file=terraform.tfvars
